# Controladores Backoffice

Esta carpeta queda preparada para mover gradualmente la logica de `admin/`.

Las URLs del panel siguen entrando por `/admin/`; cada archivo en `admin/` deberia
quedar como entrypoint delgado que delega a un controlador de esta carpeta.
