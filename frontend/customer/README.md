# Frontend Cliente

- `views/` contiene las vistas PHP del sitio publico.
- `components/` contiene componentes visuales reutilizables del cliente.
- `assets/` queda reservado para fuentes frontend si se agrega build.

No agregar consultas SQL ni reglas de negocio aca; eso va en `app/`.
